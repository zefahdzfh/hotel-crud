<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guest Book</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css" rel="stylesheet">
    <!-- Menggunakan Google Fonts untuk tampilan yang lebih modern -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
        .card {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease-in-out;
        }
        .card:hover {
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.2);
        }
        .card-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: #2d3748;
        }
        .card-text {
            font-size: 0.9rem;
            color: #4a5568;
        }
        .card-actions a {
            font-size: 0.9rem;
            text-decoration: none;
            color: #3182ce;
            transition: color 0.2s ease;
        }
        .card-actions a:hover {
            color: #2b6cb0;
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-6">
        <h1 class="text-3xl font-semibold mb-6 text-center text-gray-800">Daftar Tamu</h1>
        
        <!-- Button Tambah Tamu -->
        <div class="mb-6 text-right">
            <a href="/guestbook/create" class="bg-blue-500 text-white py-2 px-6 rounded-lg shadow-md hover:bg-blue-600">Tambah Tamu</a>
        </div>

        <!-- Card untuk Daftar Tamu -->
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            <% for (let i = 0; i < guests.length; i++) { %>
                <div class="card">
                    <h2 class="card-title"><%= guests[i].NamaTamu %></h2>
                    <p class="card-text mb-2">Email: <%= guests[i].EmailTamu %></p>
                    <p class="card-text mb-2">Telepon: <%= guests[i].PhoneTamu %></p>
                    
                    <!-- Informasi Kamar -->
                    <p class="text-gray-500 mb-2">Kamar: <%= guests[i].RoomID %></p>

                    <p class="card-text mb-2">Check-In: <%= guests[i].CheckInTime %></p>
                    <p class="card-text mb-2">Check-Out: <%= guests[i].CheckOutTime %></p>
                    <p class="card-text mb-2">Notes: <%= guests[i].Notes %></p>

                    <!-- Aksi Edit dan Hapus -->
                    <div class="card-actions mt-4 flex justify-between">
                        <form action="/guestbook/update/<%= guests[i].GuestBookID %>" method="POST">
                            <button type="submit" class="bg-yellow-500 text-white px-4 py-2 rounded-lg hover:bg-yellow-600 focus:outline-none">Edit</button>
                        </form>
                        
                       <form action="/guestbook/delete/<%= guests.GuestBookID %>?_method=DELETE" method="POST">
    <button type="submit" class="bg-red-500 text-white py-2 px-4 rounded-lg hover:bg-red-600 transition">Hapus</button>
</form>

                    </div>
                    
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
